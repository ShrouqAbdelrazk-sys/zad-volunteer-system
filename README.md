# 🚀 نظام تقييم متطوعي مشروع زاد - جاهز للنشر على Koyeb

## 📋 الملفات الجاهزة للنشر

هذا المجلد يحتوي على النظام الكامل جاهز للنشر على منصة Koyeb أو أي منصة استضافة أخرى.

## 📁 هيكل المشروع

```
/
├── server.js              # الخادم الرئيسي
├── package.json           # إعدادات NPM والـ dependencies
├── .env.example           # مثال على ملف البيئة
├── config/
│   └── db.js             # إعدادات قاعدة البيانات
├── routes/               # API Routes
│   ├── auth.js           # مسارات المصادقة
│   ├── volunteers.js     # مسارات المتطوعين
│   ├── evaluations.js    # مسارات التقييمات
│   ├── reports.js        # مسارات التقارير
│   ├── alerts.js         # مسارات التنبيهات
│   └── criteria.js       # مسارات معايير التقييم
├── public/               # ملفات Frontend
│   ├── index.html        # صفحة تسجيل الدخول
│   └── dashboard.html    # لوحة التحكم
└── README.md             # هذا الملف
```

## ⚡ النشر على Koyeb - خطوات سريعة

### 1️⃣ إنشاء مستودع GitHub

```bash
# في جهازك المحلي
git init
git add .
git commit -m "Initial commit - Zad Volunteer System"
git branch -M main
git remote add origin https://github.com/yourusername/zad-volunteer-system.git
git push -u origin main
```

### 2️⃣ إعداد قاعدة البيانات Neon

1. اذهب إلى https://neon.tech
2. إنشاء حساب مجاني
3. إنشاء قاعدة بيانات جديدة
4. احفظ الـ `DATABASE_URL` من Connection Details

### 3️⃣ النشر على Koyeb

1. اذهب إلى https://app.koyeb.com
2. انقر "Create App"
3. اختر "GitHub" كمصدر
4. اختر المستودع `zad-volunteer-system`
5. إضافة Environment Variables:
   ```
   DATABASE_URL=postgresql://username:password@ep-xxx.neon.tech/dbname?sslmode=require
   JWT_SECRET=your-super-secret-key-here
   NODE_ENV=production
   ```
6. انقر "Deploy"

## 🔧 إعدادات البيئة المطلوبة

أنشئ ملف `.env` في جذر المشروع:

```env
# قاعدة البيانات Neon
DATABASE_URL=postgresql://username:password@ep-xxx.neon.tech/dbname?sslmode=require

# إعدادات JWT
JWT_SECRET=your-super-secret-jwt-key-here-make-it-very-long
JWT_EXPIRE_IN=7d

# إعدادات أخرى
NODE_ENV=production
PORT=3000
FRONTEND_URL=*
```

## 📊 قاعدة البيانات - الجداول المطلوبة

النظام يتطلب الجداول التالية (سيتم إنشاؤها تلقائياً):

- `users` - المستخدمين والمديرين
- `volunteers` - بيانات المتطوعين  
- `evaluations` - التقييمات الشهرية
- `evaluation_criteria` - معايير التقييم
- `evaluation_details` - تفاصيل التقييمات
- `alert_records` - التنبيهات الذكية
- `freeze_records` - سجلات التجميد
- `cumulative_notes` - الملاحظات التراكمية
- `audit_trail` - سجل العمليات

## 🎯 النقاط الرئيسية للنشر

### ✅ جاهز للعمل:
- ✅ **Backend API** كامل مع Express.js
- ✅ **Frontend** مدمج في مجلد public
- ✅ **قاعدة البيانات** متوافقة مع Neon PostgreSQL
- ✅ **المصادقة** بـ JWT Tokens
- ✅ **الأمان** مع Helmet و CORS
- ✅ **الأداء** مع Compression و Rate Limiting

### 🔧 يحتاج إعداد:
- 🔄 **Environment Variables** في Koyeb
- 🔄 **قاعدة البيانات** على Neon
- 🔄 **GitHub Repository** للكود

## 🌐 URLs بعد النشر

بعد النشر الناجح:

- **الموقع الرئيسي:** `https://your-app-name.koyeb.app`
- **لوحة التحكم:** `https://your-app-name.koyeb.app/dashboard.html`
- **API Health Check:** `https://your-app-name.koyeb.app/health`
- **API Status:** `https://your-app-name.koyeb.app/api/status`

## 🔐 بيانات الدخول الافتراضية

```
Email: admin@zad.org
Password: admin123
Role: Administrator
```

## 📞 استكشاف الأخطاء

### خطأ "Cannot find module './config'"
- تأكد من وجود مجلد `config` وملف `db.js`
- تأكد من رفع جميع المجلدات على GitHub

### خطأ في قاعدة البيانات
- تأكد من صحة `DATABASE_URL`
- تأكد من إضافة `?sslmode=require` في نهاية الرابط

### خطأ في البناء
- تأكد من وجود `package.json` صحيح
- تأكد من `"start": "node server.js"` في scripts

## 🎉 النتيجة المتوقعة

بعد النشر الناجح ستحصل على:
- 🌐 موقع ويب يعمل 24/7
- 📊 لوحة تحكم كاملة
- 🔐 نظام مصادقة آمن
- 📈 API متكامل
- 🔄 قاعدة بيانات حقيقية

---

**🚀 جاهز للنشر على Koyeb!**  
*تم التطوير خصيصاً لمشروع زاد الخيري*